排錯用的 label.new
時間 字串
時間輸入方式
時間輸出方式
時間 & 點位標示線
測試時間&點位用 (****)

排錯用的 label.new
//■■■■■■■■■   ☪𒀹𒀹  排錯用的 label.new

if time == timestamp("UTC+8", 2025,07,21,23,30)    
    label.new(bar_index, low - 0.30,    
     "●● = " + str.tostring(●●) +"\n"+     
     "●● = " + str.tostring(●●)     
     , size = size.small, style = label.style_label_up)
	  
	  	  
	  
A-3	//■■■■■■■■■   ☪𒀹𒀹  //時間 字串
//時間 字串
formatTime(t) =>
    y = str.tostring(year(t))
    m = str.tostring(month(t), "00")
    d = str.tostring(dayofmonth(t), "00")
    h = str.tostring(hour(t), "00")
    min = str.tostring(minute(t), "00")	
    y + "-" + m + "-" + d + " " + h + ":" + min

B-1	//■■■■■■■■■   ☪𒀹𒀹  時間輸入方式
var DT = timestamp("UTC+8", 2025,7,18,8,30)

B-4	//■■■■■■■■■   ☪𒀹𒀹  時間輸出方式 
hr = hour(time)
min = minute(time)



	//■■■■■■■■■   ☪𒀹𒀹  //時間 & 點位標示線
h = str.tostring(hour(time), "00")
m = str.tostring(minute(time), "00")
CO_hm = h=="00"and m=="00"? color.blue :(h=="06"or h=="18")and m=="00"?color.green :h=="12" and m=="00"? color.orange: time-time[1] > 2*(time[1]-time[2])? color.red: color(na)
time_00 = h=="00" and m == "00" ? 0 : h =="12"and m == "00" ? 12 : 99
// ■■■■■■ 時間標記 & 10點標記
plot(CO_hm == color(na)? float(na): close + 0.2 , 
     color = CO_hm, title = "時間標記",  
     linewidth = 4, style =  plot.style_steplinebr ) 
plot(CO_hm == color(na)? float(na): close + 0.3 , 
     color = CO_hm, title = "時間標記",  
     linewidth = 4, style =  plot.style_steplinebr ) 




	//■■■■■■■■■   ☪𒀹𒀹  //測試時間&點位用 (****)
//signle_length_in = input.int(5)
//var signle_length = 0
//var si_value = float(na)

if aa // 輸入布林值
    signle_length := signle_length_in
    si_value := close

if signle_length>0
    signle_length := signle_length - 1
else
    si_value := float(na)

plot(si_value,
 color=color.green,
 linewidth=5,
 style=plot.style_steplinebr,
 title="■■■"
 )





